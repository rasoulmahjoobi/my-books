export type BookData = {
  bookName: string;
  author: string;
  rating: string;
  url: string;
};

export type Book = {
  id: string;
  bookName: string;
  author: string;
  rating: string;
  url: string;
};
